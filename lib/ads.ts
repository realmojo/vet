/**
 * 애드센스 광고 단위.
 *
 * 퍼블리셔 ID 는 keywordegg / yoyang / sanhu 와 같은 계정이라 새로 승인받을
 * 필요가 없다. 슬롯은 이 사이트용으로 따로 만든 것을 쓴다. 다른 사이트 슬롯을
 * 그대로 쓰면 애드센스 리포트에서 수익이 섞여 어느 사이트가 버는지 구분되지
 * 않는다.
 *
 * 슬롯 ID 가 빈 문자열이면 <Adsense> 가 아무것도 그리지 않는다.
 */
export const AD_CLIENT = "ca-pub-9130836798889522";

export const AD_SLOTS = {
  /** 비급여(글지역) — 홈·목록 화면 */
  home: "4960002960",
  /** 비급여(상단) — 본문 맨 위 */
  top: "6671756069",
  /** 비급여(CTA) — 소개글·행동 버튼 다음 */
  middle: "3758601931",
  /** 비급여(h2) — 본문 중간 h2 아래, 목록 하단 */
  bottom: "1132438596",
} as const;
