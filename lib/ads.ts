/**
 * 애드센스 광고 단위.
 *
 * 퍼블리셔 ID 는 keywordegg / yoyang / medifee / sanhu 와 같은 계정이라
 * 새로 승인받을 필요가 없다. **슬롯은 이 사이트용으로 따로 만들어야 한다.**
 * 다른 사이트 슬롯을 그대로 쓰면 애드센스 리포트에서 수익이 섞여 어느 사이트가
 * 버는지 구분되지 않는다.
 *
 * 슬롯 ID 가 빈 문자열이면 <Adsense> 가 아무것도 그리지 않는다. 그래서 지금
 * 상태로 배포해도 빈 광고 자리가 남지 않는다. 애드센스에서 단위를 만든 뒤
 * 여기에 채우면 그때부터 노출된다.
 */
export const AD_CLIENT = "ca-pub-9130836798889522";

export const AD_SLOTS = {
  /** 동물(글지역) — 홈·허브 목록 화면 */
  home: "2895474223",
  /** 동물(상단) — 본문 맨 위 */
  top: "3741652031",
  /** 동물(CTA) — 소개글·행동 버튼 다음 */
  middle: "1582392550",
  /** 동물(h2) — 본문 중간 h2 아래, 목록 하단 */
  bottom: "7956229215",
} as const;
